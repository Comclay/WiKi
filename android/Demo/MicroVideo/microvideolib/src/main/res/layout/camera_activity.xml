<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="false"
    android:background="@android:color/transparent"
    android:orientation="vertical">

    <com.comclay.microvideolib.MovieRecorderView
        android:id="@+id/movieRecorderView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="80dp"
        android:background="@android:color/transparent"
        android:orientation="vertical">

        <Button
            android:id="@+id/shoot_button"
            android:layout_width="@dimen/video_bottom_height"
            android:layout_height="@dimen/video_bottom_height"
            android:layout_gravity="center_horizontal"
            android:background="@drawable/bg_movie_add_shoot"
            android:text="开始"
            android:textColor="#20b6ff" />
    </LinearLayout>
</RelativeLayout>